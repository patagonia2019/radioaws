<%= form_for(theft) do |f| %>
  <% if theft.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(theft.errors.count, "error") %> prohibited this theft from being saved:</h2>

      <ul>
      <% theft.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
