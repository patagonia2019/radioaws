<%= form_for(archivecatalog) do |f| %>
  <% if archivecatalog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(archivecatalog.errors.count, "error") %> prohibited this archivecatalog from being saved:</h2>

      <ul>
      <% archivecatalog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :identifier %>
    <%= f.text_field :identifier %>
  </div>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :subtitle %>
    <%= f.text_field :subtitle %>
  </div>

  <div class="field">
    <%= f.label :detail %>
    <%= f.text_field :detail %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
